Now I have made significant progress in debugging the Kratos side. Here are some of my tracks/traces these days.

0. The problem was caused by a previous "fix" by another coding agent in ~/apps/kratos_line_rt/src/modules/particle/types/particle_base.h (Check the git log in ~/Seafile/seafile_sync/code/kratos to find the "fix" on July 27). The original proc_geo already processes the particle indices, and the "fix" breaks this totally by marching par.i repeatedly. Now I have fixed that and put a paragraph of comments in particle_base.h , after which I pushed the "kratos trunk". Read those, the modified code and the git commit history. 

1. Also very important: The broken particle transport actually caused a lot of memory access problems (coz this will make particles far outside the box), and caused undefined behaviors frequently. However, illegal memory accesses do not always cause exceptions on CUDA (it just fails silently in most cases, and in rare cases it reports illegal memory access via CUDA). So I found this problem with HIP-CPU debug mode (compile with: make ARCH=HIPCPU USRDIR=usr_ext/line_rt DEBUG=1 -j). The agents tried many random things to locate these occurance of failures, but was never able to find the real problem, just fabricated a lot of "root causes". Maybe one day they could know how to use gdb efficiently (there is now a .gdbinit under ~/scratch/line_rt). One of the key advantages of Kratos is that it could use HIP-CPU to significantly assist debugging, which is unfortunately still not quite possible to make the coding agents aware of.

2. On the Kratos side, I have re-written usr_ext/line_rt/photon.h, especially on typesetting, combining similar codes, and making n_scat actually into the game. The agents' previous attempts succeeded in writing the scattering kernel, but failed terribly simply on applying n_scat. This n_scat is very important in doing single-scattering tests, which tells me that the single-scattering tests works perfectly on Kratos (most photons' behavior were not destroyed by the broken transport with 1 scattering only).

3. When one or more of the input binary files are not available, the default parallel reader will report "MPI Cannot open the file ..." (src/io/binary/binary_io_mpi.cpp). This is totally irrelevant to MPI, but in previous sessions the agent was making random attempts in bothering with MPI, turning it on and off. Read the Kratos trunk docs (and even codes) before trying random things is very important, but I find it hard to make this a useful constraint on the agent.

4. A lot of dialogues took place when the agent believed that the discrepancy was caused by floating point precission, then started to play with FP32 and FP64. This always happen when I explicitly indicated (after thorough tests) that the problem is irrelevant to precision. These indications seemed to be useless, as after a while, the agent started to bother with FP32 and FP64 again. Actually Kratos can turn on full FP64 mode simply by having "PRECISION=2" in the make options , but the agents seemed to be never able to recognize this (although already in the docs/ under the Kratos home folder). How can we keep information like these well inside the agents' memory?

5. Similarly, any dimensions should have no less cells than 2 (in [mesh] n_cell_global = NX NY NZ , the three components, NX, NY, and NZ, should be integers no less than 2). Although already written in AGENTS.md as a rule, the agents still frequently bother with these dimensions.

6. Also similarly, the agents frequently assumed that the discrepancy was caused by RNG, by statistical fluctuations, etc. This is not ture.

7. The broken "fix" was introduced on July 27, which well explains why the Kratos side gave very good results (at least correct scaling relations) on July 24, but not any longer after July 27. The broken harness on the version control system says the versions are identical, and I actually used internet disks to recover the actual version before July 24. If the agents and LLMs could indeed read into the differences between versions, the debugging procedures should not be so painful and costly (1e9 tokens have been used).

8. Under ~/scratch/line_rt , for this moment, "rm test_0*.bin; ~/apps/kratos_line_rt/bin/kratos neufeld.par;  python3 compare_escaped.py --n-scat-max -1" works very well (--n-scat-max -1 means unlimited n_scat) gives consistent results. Test it. Along the same line, test different optical depths and plot the scaling of |x_peak| to a * tau_0 for me in png files.

9. Under line_rt_pipeline/docs/debug, some useful dialogues with some useful agents and models are presented. Read them and write a markdown on what has been done on debugging, sufficiently long and with **THOROUGH DETAILS**, along with this current prompt.

10. The debugging procedures has also broken a lot of other functionalities, like the re-sampling of line-center velocities seems to be missing, duplicated memeber variables, etc. Plan for further modifications and fixes.
